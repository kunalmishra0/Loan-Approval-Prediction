# Loan Approval Prediction: B.Tech Viva Guide

This guide is based on the actual repository implementation, including `src/config.py`, `src/data_loader.py`, `src/preprocessing.py`, `src/train.py`, `src/predict.py`, `app/streamlit_app.py`, saved artifacts, the EDA notebook, and project documentation.

The guide contains 120 implementation-specific questions arranged from foundational to advanced difficulty. Every question includes an excellent answer, common wrong answer, likely follow-up, and confidence tip.

## SECTION 1: PROJECT OVERVIEW

### 1. What problem does this project solve?
**Question:** What problem does this project solve?
**Excellent Answer:** It predicts whether a loan application is historically likely to be approved or rejected using applicant, financial, credit-history, and property information from the Kaggle Loan Prediction Dataset.
**Common Wrong Answer:** It approves loans automatically for a bank.
**Follow-up:** Why is this only a prediction system and not a complete lending decision system?
**Confidence Tip:** State that the project predicts historical patterns and is educational, not a regulated underwriting system.

### 2. What is the business objective?
**Question:** What is the business objective?
**Excellent Answer:** The objective is to support consistent preliminary screening and demonstrate how applicant attributes relate to historical loan outcomes.
**Common Wrong Answer:** The objective is to guarantee profitable loans.
**Follow-up:** What additional controls would be required before real financial use?
**Confidence Tip:** Separate screening support from final credit approval.

### 3. What is the machine learning objective?
**Question:** What is the machine learning objective?
**Excellent Answer:** The model predicts the binary target `Loan_Status`, where `Y` means approved and `N` means rejected.
**Common Wrong Answer:** It predicts the loan amount.
**Follow-up:** Why is this supervised learning?
**Confidence Tip:** Mention that the dataset contains historical labeled outcomes.

### 4. Why is this a classification problem?
**Question:** Why is this a classification problem?
**Excellent Answer:** The target has two discrete classes, approved and rejected, so binary classification is appropriate.
**Common Wrong Answer:** It is regression because income and loan amount are numeric.
**Follow-up:** What would change if the target were expected repayment amount?
**Confidence Tip:** Focus on the target variable, not the input variable types.

### 5. Why use machine learning instead of hard-coded rules?
**Question:** Why use machine learning instead of hard-coded rules?
**Excellent Answer:** The data contains interactions among credit history, demographics, income, loan details, and property area that are difficult to encode completely as manual rules.
**Common Wrong Answer:** Machine learning is always more accurate than rules.
**Follow-up:** When could a rule-based system be preferable?
**Confidence Tip:** Avoid claiming that ML is automatically superior.

### 6. What dataset does the project use?
**Question:** What dataset does the project use?
**Excellent Answer:** It uses the Kaggle Loan Prediction Problem Dataset, with `data/raw/train.csv` as the labeled training file and `data/raw/test.csv` as the unlabeled companion file.
**Common Wrong Answer:** It uses a synthetic dataset generated by the application.
**Follow-up:** Which file is actually used by `src.train`?
**Confidence Tip:** Mention the Kaggle attribution documented in the README.

### 7. What is the target column?
**Question:** What is the target column?
**Excellent Answer:** The target column is `Loan_Status`.
**Common Wrong Answer:** The target is `Loan_ID`.
**Follow-up:** Why is `Loan_ID` excluded?
**Confidence Tip:** Distinguish identifiers from predictive features.

### 8. Which business features are collected by the app?
**Question:** Which business features are collected by the app?
**Excellent Answer:** Gender, Married, Dependents, Education, Self Employed, Applicant Income, Coapplicant Income, Loan Amount, Loan Amount Term, Credit History, and Property Area.
**Common Wrong Answer:** Only income and loan amount.
**Follow-up:** How does the app ensure these fields match training schema?
**Confidence Tip:** Use the exact configured feature names.

### 9. Why is classification appropriate for loan approval but potentially sensitive?
**Question:** Why is classification appropriate for loan approval but potentially sensitive?
**Excellent Answer:** It matches the binary outcome, but the decision concerns access to finance, so bias, explainability, calibration, and human oversight matter.
**Common Wrong Answer:** Sensitivity is irrelevant because it is only a student project.
**Follow-up:** Which fairness analyses would you add?
**Confidence Tip:** Acknowledge responsible ML explicitly.

### 10. What is the main limitation of the dataset?
**Question:** What is the main limitation of the dataset?
**Excellent Answer:** It is a small historical dataset and may reflect past institutional policies, sampling bias, and missing context.
**Common Wrong Answer:** The only limitation is that it has categorical columns.
**Follow-up:** How could a larger dataset change the project?
**Confidence Tip:** Mention representativeness and historical bias.

### 11. What does the disclaimer in Streamlit communicate?
**Question:** What does the disclaimer in Streamlit communicate?
**Excellent Answer:** It states that the prediction is educational and must not be used for actual financial decisions.
**Common Wrong Answer:** It guarantees the prediction is correct.
**Follow-up:** Why is that disclaimer not sufficient for production governance?
**Confidence Tip:** A disclaimer does not replace validation and controls.

### 12. What would success mean for this project?
**Question:** What would success mean for this project?
**Excellent Answer:** Success means a reproducible, well-documented workflow with leakage-safe evaluation and a usable demonstration, not just a high single metric.
**Common Wrong Answer:** Success means every applicant is approved.
**Follow-up:** Which metric does the current workflow prioritize?
**Confidence Tip:** Mention both technical and project-quality success.

## SECTION 2: REPOSITORY ARCHITECTURE

### 13. What is the role of `src/config.py`?
**Question:** What is the role of `src/config.py`?
**Excellent Answer:** It centralizes paths, target name, feature lists, random seed, test size, and output directories.
**Common Wrong Answer:** It trains the model.
**Follow-up:** Why is centralizing these values better than repeating strings?
**Confidence Tip:** Point to configuration as a single source of truth.

### 14. What is the role of `src/data_loader.py`?
**Question:** What is the role of `src/data_loader.py`?
**Excellent Answer:** It loads `train.csv`, checks that the file exists, handles common CSV errors, and validates that the dataset is non-empty and contains the target.
**Common Wrong Answer:** It cleans and encodes every feature.
**Follow-up:** What validation is still missing?
**Confidence Tip:** Keep loading separate from transformation.

### 15. What is the role of `src/preprocessing.py`?
**Question:** What is the role of `src/preprocessing.py`?
**Excellent Answer:** It separates features and target and builds the numeric and categorical preprocessing transformer using sklearn components.
**Common Wrong Answer:** It compares the classifiers.
**Follow-up:** Why should it not perform model selection?
**Confidence Tip:** Emphasize single responsibility.

### 16. What is the role of `src/train.py`?
**Question:** What is the role of `src/train.py`?
**Excellent Answer:** It loads data, encodes the target, splits data, compares models with cross-validation, evaluates the winner, and saves artifacts and metadata.
**Common Wrong Answer:** It only creates charts.
**Follow-up:** Why does train-test splitting belong there?
**Confidence Tip:** Training orchestration belongs in the training entry point.

### 17. What is the role of `src/predict.py`?
**Question:** What is the role of `src/predict.py`?
**Excellent Answer:** It loads the saved pipeline, validates inference inputs, calls prediction, calculates probabilities, and decodes business labels.
**Common Wrong Answer:** It retrains the model for every request.
**Follow-up:** Why is this module reusable by both CLI and Streamlit?
**Confidence Tip:** Describe it as the inference boundary.

### 18. What is the role of `app/streamlit_app.py`?
**Question:** What is the role of `app/streamlit_app.py`?
**Excellent Answer:** It collects user input, displays metadata and results, and delegates inference to `src.predict`.
**Common Wrong Answer:** It manually scales every numeric value.
**Follow-up:** Why should the UI remain thin?
**Confidence Tip:** UI code should not own ML behavior.

### 19. Why is the repository modular?
**Question:** Why is the repository modular?
**Excellent Answer:** Each module has a focused responsibility, making testing, maintenance, replacement, and debugging easier.
**Common Wrong Answer:** Because more files always mean better software.
**Follow-up:** What downside can excessive modularity introduce?
**Confidence Tip:** Explain that modularity should follow responsibility boundaries.

### 20. What is the central artifact in the architecture?
**Question:** What is the central artifact in the architecture?
**Excellent Answer:** `models/loan_model.pkl`, which contains the fitted preprocessing and classifier in one sklearn Pipeline.
**Common Wrong Answer:** The confusion matrix image.
**Follow-up:** What human-readable artifact accompanies it?
**Confidence Tip:** Name the metadata JSON.

### 21. What is the training dependency flow?
**Question:** What is the training dependency flow?
**Excellent Answer:** `train.py` depends on `data_loader`, `preprocessing`, and `config`; the app depends on `predict`, which depends on the saved artifacts and `config`.
**Common Wrong Answer:** Every file imports every other file.
**Follow-up:** Where would you place a future API service?
**Confidence Tip:** Explain dependency direction.

### 22. Why are paths based on `PROJECT_ROOT`?
**Question:** Why are paths based on `PROJECT_ROOT`?
**Excellent Answer:** It makes paths independent of the current working directory when modules are executed from the project root or imported.
**Common Wrong Answer:** It makes files available on the internet.
**Follow-up:** What path assumptions remain in the project?
**Confidence Tip:** Be honest about how the Streamlit command is normally run.

### 23. Why are metadata and the model saved separately?
**Question:** Why are metadata and the model saved separately?
**Excellent Answer:** The binary pipeline is efficient for inference, while JSON metadata is readable by humans and the Streamlit sidebar.
**Common Wrong Answer:** JSON is faster than joblib for model prediction.
**Follow-up:** What additional metadata would improve lineage?
**Confidence Tip:** Mention dataset hash and model version.

### 24. How does the architecture support replacement of the classifier?
**Question:** How does the architecture support replacement of the classifier?
**Excellent Answer:** `build_training_pipeline` accepts a classifier and combines it with the same preprocessor, so candidate models can share the feature workflow.
**Common Wrong Answer:** The classifier is hard-coded in Streamlit.
**Follow-up:** What would need to change to add SVM?
**Confidence Tip:** Focus on the candidate-model registry.

## SECTION 3: DATA UNDERSTANDING

### 25. What columns are in the training data?
**Question:** What columns are in the training data?
**Excellent Answer:** The data includes `Loan_ID`, demographics, income fields, `LoanAmount`, `Loan_Amount_Term`, `Credit_History`, `Property_Area`, and `Loan_Status`.
**Common Wrong Answer:** Only numerical columns are present.
**Follow-up:** Which column is removed before modeling?
**Confidence Tip:** Group them as identifier, numeric, categorical, and target.

### 26. Which fields have missing values?
**Question:** Which fields have missing values?
**Excellent Answer:** The EDA shows missingness in fields including Gender, Married, Dependents, Self_Employed, LoanAmount, Loan_Amount_Term, and Credit_History.
**Common Wrong Answer:** No field has missing values.
**Follow-up:** How does the training pipeline handle them?
**Confidence Tip:** Use the EDA evidence to motivate imputation.

### 27. Why is missing Credit History important?
**Question:** Why is missing Credit History important?
**Excellent Answer:** It is both a high-signal feature and a field with missing values, so removing rows could discard valuable data and bias the sample.
**Common Wrong Answer:** Missing credit history can always be treated as good credit.
**Follow-up:** What imputation strategy does the project use?
**Confidence Tip:** Do not equate missing with positive evidence.

### 28. What does the target distribution show?
**Question:** What does the target distribution show?
**Excellent Answer:** Approved applications are the majority class, so a majority-class baseline can make accuracy look better than balanced performance.
**Common Wrong Answer:** Rejected applications are the only class.
**Follow-up:** Which metrics address this concern?
**Confidence Tip:** Mention Precision, Recall, F1, and ROC AUC.

### 29. What did the EDA show about income?
**Question:** What did the EDA show about income?
**Excellent Answer:** Income is right-skewed and has extreme values, but its standalone relationship with approval is weaker than credit history in the fitted model.
**Common Wrong Answer:** Income determines the label perfectly.
**Follow-up:** Why might income still matter in interactions?
**Confidence Tip:** Distinguish marginal patterns from model behavior.

### 30. What did the EDA show about loan amount?
**Question:** What did the EDA show about loan amount?
**Excellent Answer:** Loan amount is right-skewed, has outliers, and overlaps across approval classes, so it is informative but not deterministic.
**Common Wrong Answer:** Loan amount has no variation.
**Follow-up:** How is it handled in preprocessing?
**Confidence Tip:** Mention median imputation and scaling.

### 31. What did the EDA show about outliers?
**Question:** What did the EDA show about outliers?
**Excellent Answer:** Income and loan amount contain valid-looking right-tail observations. They should not be removed automatically without domain evidence.
**Common Wrong Answer:** Every boxplot outlier must be deleted.
**Follow-up:** Why can removing them hurt a credit model?
**Confidence Tip:** Financial data can legitimately contain high-value applicants.

### 32. What does the correlation heatmap tell you?
**Question:** What does the correlation heatmap tell you?
**Excellent Answer:** It shows linear relationships among numeric features and a temporary numeric representation of the target; it does not capture all categorical or nonlinear effects.
**Common Wrong Answer:** It proves the exact feature-importance ranking.
**Follow-up:** Why is correlation not causal evidence?
**Confidence Tip:** Avoid treating heatmap values as model explanations.

### 33. What feature appears most associated with approval in EDA?
**Question:** What feature appears most associated with approval in EDA?
**Excellent Answer:** Credit history has the strongest numeric association with approval and visibly separates outcomes more than income alone.
**Common Wrong Answer:** Loan ID has the strongest association because it is unique.
**Follow-up:** Does correlation alone justify dropping all other features?
**Confidence Tip:** Explain complementary signal.

### 34. What did the EDA show about categorical distributions?
**Question:** What did the EDA show about categorical distributions?
**Excellent Answer:** Categories have unequal frequencies and some missing values, supporting imputation and one-hot encoding.
**Common Wrong Answer:** All categories are equally frequent.
**Follow-up:** How does OneHotEncoder handle unseen categories?
**Confidence Tip:** Connect EDA to preprocessing.

### 35. Why is EDA not part of `train.py`?
**Question:** Why is EDA not part of `train.py`?
**Excellent Answer:** EDA is an analysis and documentation activity, while `train.py` should be a repeatable training workflow without interactive exploratory plots.
**Common Wrong Answer:** Plots cannot be made in Python scripts.
**Follow-up:** Where is EDA documented?
**Confidence Tip:** Name `notebooks/01_exploratory_data_analysis.ipynb`.

### 36. What is the difference between EDA observations and learned coefficients?
**Question:** What is the difference between EDA observations and learned coefficients?
**Excellent Answer:** EDA describes dataset-level patterns, while coefficients describe the fitted Logistic Regression under its preprocessing and training split.
**Common Wrong Answer:** They are exactly the same thing.
**Follow-up:** Why should both be interpreted cautiously?
**Confidence Tip:** State that neither proves causation.

## SECTION 4: PREPROCESSING

### 37. Why use median imputation for numeric features?
**Question:** Why use median imputation for numeric features?
**Excellent Answer:** Median is robust to skew and extreme values seen in income and loan amount, and it preserves rows.
**Common Wrong Answer:** Median is used because numerical values cannot be missing.
**Follow-up:** What risk does fitting the median on all data create?
**Confidence Tip:** Tie the answer to leakage.

### 38. Why use mode imputation for categorical features?
**Question:** Why use mode imputation for categorical features?
**Excellent Answer:** The most frequent category is a simple, reproducible replacement that maintains the expected categorical type.
**Common Wrong Answer:** Mode converts categories into numbers.
**Follow-up:** What alternative could preserve missingness explicitly?
**Confidence Tip:** Mention a missing category or indicator.

### 39. Why use OneHotEncoder?
**Question:** Why use OneHotEncoder?
**Excellent Answer:** It represents nominal categories without creating an artificial numeric order, such as treating Rural as 1 and Urban as 3.
**Common Wrong Answer:** It sorts categories by financial risk.
**Follow-up:** What does `handle_unknown='ignore'` do?
**Confidence Tip:** Use an example of a new category at inference.

### 40. Why is `drop='if_binary'` configured?
**Question:** Why is `drop='if_binary'` configured?
**Excellent Answer:** It removes one redundant level for binary categorical variables while retaining multi-category information.
**Common Wrong Answer:** It drops every categorical column.
**Follow-up:** What happens to multi-category Dependents?
**Confidence Tip:** Explain that its levels remain represented.

### 41. Why use ColumnTransformer?
**Question:** Why use ColumnTransformer?
**Excellent Answer:** It routes numeric columns through imputation and scaling and categorical columns through imputation and encoding in one transformer.
**Common Wrong Answer:** It is only a plotting utility.
**Follow-up:** What would be risky about manually preprocessing each field?
**Confidence Tip:** Mention drift and inconsistent inference.

### 42. Why use Pipeline?
**Question:** Why use Pipeline?
**Excellent Answer:** It fits transformations only on the data passed to each training fold and keeps the exact transformation sequence attached to the classifier.
**Common Wrong Answer:** It makes the model more accurate automatically.
**Follow-up:** How does this help deployment?
**Confidence Tip:** Answer in terms of one artifact and one predict call.

### 43. Where does preprocessing fit relative to the classifier?
**Question:** Where does preprocessing fit relative to the classifier?
**Excellent Answer:** The pipeline order is preprocessor first, classifier second.
**Common Wrong Answer:** The classifier runs before imputation.
**Follow-up:** What does `pipeline.predict(user_input)` trigger?
**Confidence Tip:** Describe transform followed by predict.

### 44. How is data leakage prevented during cross-validation?
**Question:** How is data leakage prevented during cross-validation?
**Excellent Answer:** Each fold fits its own preprocessor using only that fold's training portion because the preprocessor is inside the estimator Pipeline.
**Common Wrong Answer:** The test data is manually cleaned before CV.
**Follow-up:** Would fitting a scaler before cross-validation leak information?
**Confidence Tip:** Say yes, through distribution statistics.

### 45. Why is train-test split in `train.py`?
**Question:** Why is train-test split in `train.py`?
**Excellent Answer:** Splitting is part of experimental design and model evaluation, not a reusable column-transformation responsibility.
**Common Wrong Answer:** Because preprocessing cannot import sklearn.
**Follow-up:** What data does the preprocessor see first?
**Confidence Tip:** It sees only the relevant training fold or training split.

### 46. Why use stratify in train_test_split?
**Question:** Why use stratify in train_test_split?
**Excellent Answer:** It preserves the approved/rejected class proportions in both train and holdout subsets.
**Common Wrong Answer:** It sorts rows alphabetically.
**Follow-up:** Why is this especially useful for a small imbalanced dataset?
**Confidence Tip:** Mention stable class representation.

### 47. Why scale only numeric features?
**Question:** Why scale only numeric features?
**Excellent Answer:** Scaling is meaningful for numeric magnitudes; categorical values are represented through one-hot indicators instead of being treated as continuous quantities.
**Common Wrong Answer:** Categorical values are already in dollars.
**Follow-up:** Why is scaling useful for Logistic Regression?
**Confidence Tip:** Discuss optimization and comparable numeric scales.

### 48. Does the standalone `preprocess_train_test` function leak data?
**Question:** Does the standalone `preprocess_train_test` function leak data?
**Excellent Answer:** Its implementation fits on training features and transforms test features, so the operation itself is leakage-safe, but the active training flow correctly uses complete pipelines instead.
**Common Wrong Answer:** It fits on train and test together.
**Follow-up:** Why might keeping two preprocessing APIs be confusing?
**Confidence Tip:** Mention maintenance and misuse risk.

## SECTION 5: MODEL TRAINING

### 49. Which models are trained?
**Question:** Which models are trained?
**Excellent Answer:** Logistic Regression, Decision Tree, Random Forest, and XGBoost are defined in `build_models()`.
**Common Wrong Answer:** Only XGBoost is trained.
**Follow-up:** Why include both linear and tree models?
**Confidence Tip:** List all four exactly.

### 50. Why include Logistic Regression?
**Question:** Why include Logistic Regression?
**Excellent Answer:** It is an efficient, interpretable binary-classification baseline and works naturally with scaled and one-hot features.
**Common Wrong Answer:** Because it always beats boosting.
**Follow-up:** What do its coefficients tell you?
**Confidence Tip:** Say association with the encoded approval class, not causality.

### 51. Why include Decision Tree?
**Question:** Why include Decision Tree?
**Excellent Answer:** It captures nonlinear thresholds and interactions without needing scaled numeric features.
**Common Wrong Answer:** It is guaranteed not to overfit.
**Follow-up:** What is its major risk?
**Confidence Tip:** State overfitting and high variance.

### 52. Why include Random Forest?
**Question:** Why include Random Forest?
**Excellent Answer:** It averages many randomized trees, usually reducing the variance of a single tree and offering a strong nonlinear baseline.
**Common Wrong Answer:** It is a single very deep tree.
**Follow-up:** Why is `n_estimators=200` used?
**Confidence Tip:** Explain the bias-variance and compute tradeoff.

### 53. Why include XGBoost?
**Question:** Why include XGBoost?
**Excellent Answer:** It provides a gradient-boosted tree comparison that can model nonlinearities and interactions effectively on tabular data.
**Common Wrong Answer:** It is required for every classification problem.
**Follow-up:** What does `eval_metric='logloss'` control?
**Confidence Tip:** Describe the training evaluation objective.

### 54. What hyperparameters are explicitly configured?
**Question:** What hyperparameters are explicitly configured?
**Excellent Answer:** Logistic Regression uses `max_iter=1000`; Random Forest uses `n_estimators=200`; XGBoost uses learning rate 0.05, max depth 3, 200 estimators, logloss, and parallel jobs; random seeds are configured where relevant.
**Common Wrong Answer:** Every hyperparameter is tuned by GridSearchCV.
**Follow-up:** Why is this not hyperparameter tuning?
**Confidence Tip:** Be precise about fixed candidate settings.

### 55. Why compare multiple model families?
**Question:** Why compare multiple model families?
**Excellent Answer:** Different models make different assumptions, so comparison tests whether a linear or nonlinear learner performs better on these features.
**Common Wrong Answer:** To make the project look longer.
**Follow-up:** What selection criterion is used?
**Confidence Tip:** F1 first, ROC AUC second.

### 56. How does cross-validation work here?
**Question:** How does cross-validation work here?
**Excellent Answer:** `StratifiedKFold` with five shuffled folds evaluates each pipeline on held-out fold data while preserving class proportions.
**Common Wrong Answer:** It evaluates only the final test set five times.
**Follow-up:** Which data is used for CV?
**Confidence Tip:** Only `x_train` and `y_train`.

### 57. Why should the holdout test set not select the model?
**Question:** Why should the holdout test set not select the model?
**Excellent Answer:** Using it repeatedly for selection makes it part of model development and causes optimistic evaluation.
**Common Wrong Answer:** Because the test set has no labels.
**Follow-up:** When is the holdout set used in this project?
**Confidence Tip:** Only after model selection.

### 58. How is the best model selected?
**Question:** How is the best model selected?
**Excellent Answer:** The comparison DataFrame is sorted descending by F1 Score and then ROC AUC, and the first algorithm is selected automatically.
**Common Wrong Answer:** Logistic Regression is hard-coded as the winner.
**Follow-up:** What would happen if XGBoost scored higher?
**Confidence Tip:** It would be selected by the same mechanism.

### 59. Why are Random Forest and XGBoost given `n_jobs=-1`?
**Question:** Why are Random Forest and XGBoost given `n_jobs=-1`?
**Excellent Answer:** They can parallelize independent tree computations across available CPU cores.
**Common Wrong Answer:** It increases the number of features.
**Follow-up:** Why might nested parallelism need caution?
**Confidence Tip:** Mention resource contention.

### 60. What does `train_single_model` return?
**Question:** What does `train_single_model` return?
**Excellent Answer:** It returns a fitted complete sklearn Pipeline containing a fresh preprocessor and the requested classifier.
**Common Wrong Answer:** It returns only a list of predictions.
**Follow-up:** Why is returning the complete pipeline important for saving?
**Confidence Tip:** Connect training to deployment.

## SECTION 6: EVALUATION

### 61. What does Accuracy measure here?
**Question:** What does Accuracy measure here?
**Excellent Answer:** Accuracy is the fraction of all predictions that are correct.
**Common Wrong Answer:** Accuracy is the probability of approval.
**Follow-up:** Why can it mislead with class imbalance?
**Confidence Tip:** Use the majority-class example.

### 62. What is Precision for this project?
**Question:** What is Precision for this project?
**Excellent Answer:** Precision measures how many applications predicted as approved are actually approved, using encoded approval as the positive class.
**Common Wrong Answer:** Precision measures all correctly rejected applications.
**Follow-up:** What is the cost of low precision?
**Confidence Tip:** Discuss false approvals.

### 63. What is Recall for this project?
**Question:** What is Recall for this project?
**Excellent Answer:** Recall measures how many truly approved applications the model identifies.
**Common Wrong Answer:** Recall measures the percentage of numeric values imputed.
**Follow-up:** What does low recall mean operationally?
**Confidence Tip:** Mention missed approvals.

### 64. What is F1 Score?
**Question:** What is F1 Score?
**Excellent Answer:** F1 is the harmonic mean of precision and recall, balancing false approvals and missed approvals.
**Common Wrong Answer:** F1 is the average of accuracy and ROC AUC.
**Follow-up:** Why is F1 the primary selection metric here?
**Confidence Tip:** Mention the imbalanced target and competing errors.

### 65. What is ROC AUC?
**Question:** What is ROC AUC?
**Excellent Answer:** ROC AUC measures ranking quality across classification thresholds using positive-class scores.
**Common Wrong Answer:** It is the accuracy at threshold 0.5.
**Follow-up:** Why can ROC AUC be useful before choosing a threshold?
**Confidence Tip:** Separate ranking from thresholded classification.

### 66. What are the current holdout metrics?
**Question:** What are the current holdout metrics?
**Excellent Answer:** The saved metadata reports Accuracy 0.862, Precision 0.840, Recall 0.988, F1 Score 0.908, and ROC AUC 0.853.
**Common Wrong Answer:** The model has 100% accuracy.
**Follow-up:** Why should these numbers be interpreted cautiously?
**Confidence Tip:** Mention the small holdout size and historical data.

### 67. Which model is currently selected?
**Question:** Which model is currently selected?
**Excellent Answer:** The metadata identifies Logistic Regression as the best model under the CV ranking and final saved artifact.
**Common Wrong Answer:** XGBoost is always selected because it is most complex.
**Follow-up:** Would the code still work if another model won?
**Confidence Tip:** Explain automatic selection.

### 68. What is a confusion matrix?
**Question:** What is a confusion matrix?
**Excellent Answer:** It counts true negatives, false positives, false negatives, and true positives for the final holdout predictions.
**Common Wrong Answer:** It is a correlation matrix.
**Follow-up:** Where is it saved?
**Confidence Tip:** Name `reports/figures/confusion_matrix.png`.

### 69. What is the classification threshold?
**Question:** What is the classification threshold?
**Excellent Answer:** For classifiers exposing probabilities, the default prediction threshold is generally 0.5 unless changed; this project does not tune a business-specific threshold.
**Common Wrong Answer:** The threshold is learned from the dataset automatically.
**Follow-up:** How could changing it affect precision and recall?
**Confidence Tip:** Explain the tradeoff.

### 70. Why might high recall be desirable?
**Question:** Why might high recall be desirable?
**Excellent Answer:** High recall reduces missed approvals, which may matter if false rejection is costly.
**Common Wrong Answer:** High recall means there are no false approvals.
**Follow-up:** What risk can very high recall introduce?
**Confidence Tip:** Discuss lower precision and false approvals.

### 71. Why might precision matter to a lender?
**Question:** Why might precision matter to a lender?
**Excellent Answer:** High precision reduces the number of applications predicted approved that are actually rejected historically, potentially reducing false-approval risk.
**Common Wrong Answer:** Precision only matters for image classification.
**Follow-up:** Which business cost should determine its priority?
**Confidence Tip:** Frame metrics as cost-sensitive choices.

### 72. Why not use Accuracy alone to select the model?
**Question:** Why not use Accuracy alone to select the model?
**Excellent Answer:** The approved class is the majority, and Accuracy can hide poor performance on the minority class or asymmetric business costs.
**Common Wrong Answer:** Accuracy cannot be computed for binary data.
**Follow-up:** What other metric would you inspect for ranking?
**Confidence Tip:** Answer ROC AUC plus class-specific metrics.

## SECTION 7: IMPLEMENTATION

### 73. Why save the complete Pipeline instead of only the classifier?
**Question:** Why save the complete Pipeline instead of only the classifier?
**Excellent Answer:** The saved object contains the fitted imputers, scaler, encoder, feature ordering, and classifier, so inference applies exactly the same transformations as training.
**Common Wrong Answer:** Only the classifier is needed because the UI already cleans the data.
**Follow-up:** What breaks if the encoder is recreated independently?
**Confidence Tip:** Describe feature-order and category mismatch.

### 74. Why use `predict.py`?
**Question:** Why use `predict.py`?
**Excellent Answer:** It creates one reusable inference contract for the CLI and Streamlit, centralizing model loading, schema validation, label decoding, and probability handling.
**Common Wrong Answer:** It is just a second training script.
**Follow-up:** How does this reduce UI duplication?
**Confidence Tip:** Point to the direct pipeline call.

### 75. Why use joblib?
**Question:** Why use joblib?
**Excellent Answer:** joblib is suitable for serializing sklearn estimators and NumPy-backed objects efficiently.
**Common Wrong Answer:** joblib is a database.
**Follow-up:** What security warning applies to joblib files?
**Confidence Tip:** Mention trusted-artifact-only loading.

### 76. Why use `model_metadata.json`?
**Question:** Why use `model_metadata.json`?
**Excellent Answer:** It exposes algorithm, metrics, sample counts, feature names, target mappings, versions, and timestamp without loading the binary model for every display concern.
**Common Wrong Answer:** It stores the raw CSV.
**Follow-up:** What lineage fields are missing?
**Confidence Tip:** Mention dataset hash and commit.

### 77. Why use logging instead of print in the main workflow?
**Question:** Why use logging instead of print in the main workflow?
**Excellent Answer:** Logging records severity, timestamps, and context and can be redirected or configured in different environments.
**Common Wrong Answer:** Logging is always faster than print.
**Follow-up:** What is imperfect about the current logging setup?
**Confidence Tip:** Mention that modules call `basicConfig`, which is global.

### 78. Why are helper functions used in `train.py`?
**Question:** Why are helper functions used in `train.py`?
**Excellent Answer:** Functions such as `prepare_target`, `split_data`, `build_models`, `evaluate_model`, and `save_metadata` isolate behavior and make testing and review easier.
**Common Wrong Answer:** It avoids writing any code in main.
**Follow-up:** Which function owns model selection?
**Confidence Tip:** Name `compare_models`.

### 79. Why does `config.py` define feature lists?
**Question:** Why does `config.py` define feature lists?
**Excellent Answer:** The same schema is needed by preprocessing and inference, so central lists reduce feature-order mismatch.
**Common Wrong Answer:** Feature lists are only for display.
**Follow-up:** What happens if app input uses a different spelling?
**Confidence Tip:** It fails required-feature validation or is not selected.

### 80. Why store transformed feature names?
**Question:** Why store transformed feature names?
**Excellent Answer:** They document the post-encoding schema and help interpret coefficients or inspect the fitted transformer.
**Common Wrong Answer:** They are needed to run Streamlit.
**Follow-up:** Where are they generated?
**Confidence Tip:** From `get_transformed_feature_names`.

### 81. Why does `predict` return both probability and confidence?
**Question:** Why does `predict` return both probability and confidence?
**Excellent Answer:** Approval probability represents the positive approval class, while confidence represents the probability of the selected class.
**Common Wrong Answer:** They always mean exactly the same thing.
**Follow-up:** Why was this distinction important in this project?
**Confidence Tip:** Mention the earlier rejected-case display bug.

### 82. Why is the raw prediction retained?
**Question:** Why is the raw prediction retained?
**Excellent Answer:** It preserves the classifier's encoded output for debugging while the business-friendly prediction is shown to users.
**Common Wrong Answer:** Raw output is more understandable to applicants.
**Follow-up:** How is it decoded?
**Confidence Tip:** Through metadata mapping and `APPROVAL_LABELS`.

### 83. Why cache the model in Streamlit?
**Question:** Why cache the model in Streamlit?
**Excellent Answer:** Loading the serialized pipeline once avoids repeated disk I/O during Streamlit reruns and improves responsiveness.
**Common Wrong Answer:** Caching retrains the model faster.
**Follow-up:** What stale-cache issue should be addressed?
**Confidence Tip:** Mention model modification time or manual cache clearing.

### 84. Why does `main()` remain small?
**Question:** Why does `main()` remain small?
**Excellent Answer:** It orchestrates the workflow while helper functions own details, which improves readability and testability.
**Common Wrong Answer:** Small main functions cannot fail.
**Follow-up:** Which exceptions are logged at the app boundary?
**Confidence Tip:** The Streamlit initialization and prediction blocks catch and log exceptions.

## SECTION 8: DEPLOYMENT

### 85. What is the Streamlit prediction flow?
**Question:** What is the Streamlit prediction flow?
**Excellent Answer:** The user fills the form, Streamlit builds a dictionary, calls `src.predict.predict`, which validates input and calls the cached saved pipeline, then the UI displays the decoded result.
**Common Wrong Answer:** Streamlit calls `src.train` for every button click.
**Follow-up:** Where is the model loaded?
**Confidence Tip:** Through `predict.load_pipeline`, cached by Streamlit.

### 86. Does Streamlit manually preprocess inputs?
**Question:** Does Streamlit manually preprocess inputs?
**Excellent Answer:** No. It collects values and delegates preprocessing to the saved sklearn Pipeline through `src.predict`.
**Common Wrong Answer:** Yes, it manually applies StandardScaler.
**Follow-up:** Why is manual preprocessing dangerous?
**Confidence Tip:** Mention train-serving skew.

### 87. How does model loading work?
**Question:** How does model loading work?
**Excellent Answer:** `load_pipeline` checks that the model path exists, calls `joblib.load`, and verifies the loaded object is an sklearn Pipeline.
**Common Wrong Answer:** It downloads a new model from Kaggle at every request.
**Follow-up:** What compatibility checks are still desirable?
**Confidence Tip:** Mention versions, schema, and checksum.

### 88. What does `validate_input` check?
**Question:** What does `validate_input` check?
**Excellent Answer:** It accepts a dictionary or DataFrame, rejects empty input, checks all configured feature names are present, and returns the required columns in model order.
**Common Wrong Answer:** It verifies the applicant's bank balance externally.
**Follow-up:** What value validation is missing?
**Confidence Tip:** Mention negative values, finite numbers, and allowed categories.

### 89. Why are selectboxes used for categorical fields?
**Question:** Why are selectboxes used for categorical fields?
**Excellent Answer:** They constrain UI input to known categories and reduce spelling and schema errors.
**Common Wrong Answer:** They allow arbitrary categories.
**Follow-up:** Can direct API callers still bypass those constraints?
**Confidence Tip:** Yes, which is why validation belongs in `predict.py` too.

### 90. Why are income and loan amount inputs constrained to nonnegative values?
**Question:** Why are income and loan amount inputs constrained to nonnegative values?
**Excellent Answer:** Negative financial amounts are invalid for the intended domain and would be nonsensical model inputs.
**Common Wrong Answer:** Negative values improve accuracy.
**Follow-up:** Does the UI alone guarantee API safety?
**Confidence Tip:** No.

### 91. What happens if the model file is missing?
**Question:** What happens if the model file is missing?
**Excellent Answer:** `load_pipeline` raises a meaningful FileNotFoundError, and Streamlit displays an initialization error instructing the user to train first.
**Common Wrong Answer:** The app silently trains a replacement.
**Follow-up:** What happens if metadata is missing?
**Confidence Tip:** The prediction module warns and can continue with an empty metadata dictionary.

### 92. Why is `handle_unknown='ignore'` useful in deployment?
**Question:** Why is `handle_unknown='ignore'` useful in deployment?
**Excellent Answer:** An unseen category will not crash the encoder, and its encoded representation will remain valid.
**Common Wrong Answer:** It automatically learns the correct business meaning of the new category.
**Follow-up:** What is the tradeoff?
**Confidence Tip:** Unknown values may reduce predictive quality and should be monitored.

### 93. How does the CLI use the same inference path?
**Question:** How does the CLI use the same inference path?
**Excellent Answer:** `predict.py` creates a sample application, loads the pipeline and metadata, calls `predict`, and prints class, probability, and confidence.
**Common Wrong Answer:** The CLI has a separate copy of the model logic.
**Follow-up:** Why is that useful for debugging?
**Confidence Tip:** It isolates UI issues from model behavior.

### 94. What does the app display besides the class?
**Question:** What does the app display besides the class?
**Excellent Answer:** It displays approval probability, confidence, model metadata and metrics in the sidebar, and the educational disclaimer.
**Common Wrong Answer:** Only a raw integer.
**Follow-up:** Why should probability be labeled carefully?
**Confidence Tip:** It is an estimate, not a guarantee.

### 95. What deployment issue exists with the cached pipeline?
**Question:** What deployment issue exists with the cached pipeline?
**Excellent Answer:** Retraining the file while the app process is running may leave the old cached object active.
**Common Wrong Answer:** Caching deletes the model.
**Follow-up:** How would you solve it?
**Confidence Tip:** Use an artifact version or modification time in the cache key.

### 96. Would this be ready for a production lending API?
**Question:** Would this be ready for a production lending API?
**Excellent Answer:** No. It needs stronger schema validation, security, calibration, fairness review, monitoring, auditability, and regulatory controls.
**Common Wrong Answer:** Yes, because the app runs locally.
**Follow-up:** What would be the first production hardening step?
**Confidence Tip:** Prioritize artifact trust and input validation.

## SECTION 9: CODE WALKTHROUGH

### 97. What happens in `train.main()`?
**Question:** What happens in `train.main()`?
**Excellent Answer:** It creates output directories, loads data, splits features and target, encodes the target, creates the split, compares candidate models using CV, evaluates the selected pipeline on holdout data, saves the confusion matrix, builds metadata, and saves artifacts.
**Common Wrong Answer:** It only calls Logistic Regression.
**Follow-up:** Which step must happen before the final evaluation?
**Confidence Tip:** Model selection must be completed first.

### 98. What does `prepare_target()` do?
**Question:** What does `prepare_target()` do?
**Excellent Answer:** It rejects null targets and single-class targets, applies LabelEncoder, and returns encoded labels plus a mapping from original labels to integers.
**Common Wrong Answer:** It imputes missing feature values.
**Follow-up:** What assumption does this function make?
**Confidence Tip:** It assumes the encoded approval class is correctly identified.

### 99. What does `split_data()` guarantee?
**Question:** What does `split_data()` guarantee?
**Excellent Answer:** It uses the configured test size and random seed and stratifies on the target.
**Common Wrong Answer:** It fits the preprocessor before splitting.
**Follow-up:** Why is that order important?
**Confidence Tip:** Splitting first helps protect evaluation.

### 100. What does `build_models()` return?
**Question:** What does `build_models()` return?
**Excellent Answer:** A dictionary mapping display names to configured estimator instances for four classifiers.
**Common Wrong Answer:** A fitted model dictionary.
**Follow-up:** Are these models tuned?
**Confidence Tip:** No, they use fixed project settings.

### 101. What does `build_training_pipeline()` assemble?
**Question:** What does `build_training_pipeline()` assemble?
**Excellent Answer:** A two-step sklearn Pipeline: `preprocessor` followed by `classifier`.
**Common Wrong Answer:** A Pipeline containing only the classifier.
**Follow-up:** Why build a fresh preprocessor for each model?
**Confidence Tip:** Each CV fit must learn transformations independently.

### 102. What does `cross_validate_model()` evaluate?
**Question:** What does `cross_validate_model()` evaluate?
**Excellent Answer:** It builds a pipeline and runs five-fold stratified cross-validation with Accuracy, Precision, Recall, F1, and ROC AUC scoring.
**Common Wrong Answer:** It evaluates on `x_test`.
**Follow-up:** Does it return fold-level standard deviations?
**Confidence Tip:** No; it currently returns fold means.

### 103. What does `compare_models()` do after collecting results?
**Question:** What does `compare_models()` do after collecting results?
**Excellent Answer:** It sorts results by F1 Score and ROC AUC, identifies the best algorithm, then fits that candidate on the full training split.
**Common Wrong Answer:** It selects based on the test set.
**Follow-up:** What is the purpose of the legacy signature block?
**Confidence Tip:** Backward compatibility, though it reduces clarity.

### 104. What does `evaluate_model()` use for ROC AUC?
**Question:** What does `evaluate_model()` use for ROC AUC?
**Excellent Answer:** It obtains positive-class scores from `predict_proba`, `decision_function`, or predictions and passes them to `roc_auc_score`.
**Common Wrong Answer:** It uses only predicted class labels for every classifier.
**Follow-up:** What class-index assumption exists?
**Confidence Tip:** It assumes column/index 1 is the positive class.

### 105. What does `_build_metadata()` record?
**Question:** What does `_build_metadata()` record?
**Excellent Answer:** Algorithm, final metrics, train/test sample counts, transformed feature names, target mappings, confusion-matrix path, timestamp, sklearn version, Python version, and CV comparison.
**Common Wrong Answer:** Only the model filename.
**Follow-up:** What would improve experiment lineage?
**Confidence Tip:** Dataset hash, dependency versions, config, and Git commit.

### 106. What does `load_pipeline()` verify?
**Question:** What does `load_pipeline()` verify?
**Excellent Answer:** It verifies the file exists and the loaded object is an sklearn Pipeline.
**Common Wrong Answer:** It verifies accuracy before loading.
**Follow-up:** What security risk remains?
**Confidence Tip:** Untrusted pickle/joblib deserialization.

### 107. What does `decode_prediction()` use?
**Question:** What does `decode_prediction()` use?
**Excellent Answer:** It uses inverse target metadata mapping and `APPROVAL_LABELS` to turn encoded outputs or Y/N values into business-friendly labels.
**Common Wrong Answer:** It asks the user which label is correct.
**Follow-up:** How could this become brittle?
**Confidence Tip:** Hardcoded positive labels and missing metadata.

### 108. What does `display_prediction()` do?
**Question:** What does `display_prediction()` do?
**Excellent Answer:** It checks the business-friendly label, uses `st.success` or `st.error`, displays probability and confidence metrics, and shows the disclaimer.
**Common Wrong Answer:** It changes the classifier threshold during display.
**Follow-up:** Where should business threshold logic live if introduced?
**Confidence Tip:** In a tested inference or decision-policy layer, not presentation formatting.

## SECTION 10: ADVANCED AND PRODUCTION QUESTIONS

### 109. Could the current workflow leak target information?
**Question:** Could the current workflow leak target information?
**Excellent Answer:** The active pipeline does not use the target in feature transformations and fits preprocessing within training folds. The main leakage protection is sound.
**Common Wrong Answer:** Yes, because every model uses the same pipeline.
**Follow-up:** What future feature could create leakage?
**Confidence Tip:** Any feature derived after the approval decision or from the target.

### 110. Is the probability calibrated?
**Question:** Is the probability calibrated?
**Excellent Answer:** Not explicitly. The project reports classifier probabilities but does not evaluate reliability or apply calibration.
**Common Wrong Answer:** Yes, every `predict_proba` output is calibrated.
**Follow-up:** How would you test calibration?
**Confidence Tip:** Use reliability diagrams and Brier score.

### 111. Why is calibration important here?
**Question:** Why is calibration important here?
**Excellent Answer:** The UI presents approval probability, so users may interpret it as a real likelihood. Poor calibration can make numerical probabilities misleading.
**Common Wrong Answer:** Calibration only changes the class labels.
**Follow-up:** Would calibration alter ranking?
**Confidence Tip:** It can preserve or change ranking depending on method.

### 112. What fairness concern exists?
**Question:** What fairness concern exists?
**Excellent Answer:** The model uses demographic and socioeconomic fields and learns from historical decisions that may encode institutional bias.
**Common Wrong Answer:** Fairness is guaranteed because Logistic Regression is linear.
**Follow-up:** Which subgroup metrics would you report?
**Confidence Tip:** Compare approval rates and error rates by relevant groups.

### 113. Why is model security important?
**Question:** Why is model security important?
**Excellent Answer:** `joblib.load` deserializes pickle-compatible objects, so a malicious artifact could execute code when loaded.
**Common Wrong Answer:** The model is secure because it is only a few kilobytes.
**Follow-up:** What practical control would you add?
**Confidence Tip:** Trusted artifact storage, checksum verification, and version validation.

### 114. What is model drift in this application?
**Question:** What is model drift in this application?
**Excellent Answer:** Drift is a change in applicant populations, feature distributions, category frequencies, or the relationship between features and approvals over time.
**Common Wrong Answer:** Drift means the Python file moved directories.
**Follow-up:** Which signals would you monitor?
**Confidence Tip:** Missingness, category distribution, income/loan distributions, and prediction rates.

### 115. Why is monitoring needed if the model is saved?
**Question:** Why is monitoring needed if the model is saved?
**Excellent Answer:** A saved artifact remains unchanged while the real-world data-generating process can change, making its predictions less reliable.
**Common Wrong Answer:** Monitoring is only for server uptime.
**Follow-up:** What outcome-based monitoring is difficult here?
**Confidence Tip:** True labels may arrive later or be unavailable.

### 116. How would you productionize the inference layer?
**Question:** How would you productionize the inference layer?
**Excellent Answer:** Wrap the logic in a versioned API such as FastAPI, validate request schemas, load a trusted versioned artifact, log requests safely, and expose health checks.
**Common Wrong Answer:** Copy the Streamlit code into a larger script.
**Follow-up:** What should never be logged?
**Confidence Tip:** Sensitive applicant data unless strictly necessary and protected.

### 117. What would CI/CD test?
**Question:** What would CI/CD test?
**Excellent Answer:** It should run syntax checks, unit tests, integration prediction tests, linting, dependency checks, and possibly notebook validation before deployment.
**Common Wrong Answer:** It should retrain randomly on every commit without checks.
**Follow-up:** What artifact should CI validate?
**Confidence Tip:** That the expected pipeline schema and prediction contract remain valid.

### 118. How would you improve experiment reproducibility?
**Question:** How would you improve experiment reproducibility?
**Excellent Answer:** Record dataset hash, source commit, complete dependency versions, configuration, model parameters, and random seeds in metadata.
**Common Wrong Answer:** Set one random seed and reproducibility is perfect.
**Follow-up:** Why can parallel libraries still complicate determinism?
**Confidence Tip:** Thread scheduling and implementation details can vary.

### 119. How would you add domain features safely?
**Question:** How would you add domain features safely?
**Excellent Answer:** Create features such as total income and loan-to-income ratio inside a tested transformer or feature-engineering step within the Pipeline, then compare them using the same CV protocol.
**Common Wrong Answer:** Compute them once on the entire dataset before CV.
**Follow-up:** Why is the latter risky?
**Confidence Tip:** Feature derivation can accidentally use unavailable or post-decision information.

### 120. What is your strongest final defense of this project?
**Question:** What is your strongest final defense of this project?
**Excellent Answer:** It demonstrates a complete and modular ML workflow with leakage-aware preprocessing, stratified validation, holdout evaluation, serialized deployment artifact, inference abstraction, documentation, and a working UI.
**Common Wrong Answer:** It is perfect and needs no further validation.
**Follow-up:** What would you improve first?
**Confidence Tip:** Choose input validation, tests, calibration, and fairness based on production risk.

